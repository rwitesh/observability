services:
  starrocks:
    image: starrocks/allin1-ubuntu
    container_name: quickstart
    ports:
      - "9030:9030"   # MySQL protocol (FE)
      - "8030:8030"   # FE web UI
      - "8040:8040"   # BE web UI
    restart: unless-stopped
